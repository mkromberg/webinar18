r←{mode}Build folder;dev;opts;parentfolder

 :If 0=⎕NC'mode' ⋄ mode←'prod' ⋄ :EndIf
 'Mode must be one of: dev prod'⎕SIGNAL((⊂mode)∊'dev' 'prod')↓11
 dev←mode≡'dev'

 r←⊂mode, ' workspace built from ',folder

 parentfolder←1⊃⎕NPARTS folder
 (opts←⎕NS'').watch←(1+dev)⊃'none' 'both'

r,←⊂ opts ⎕SE.Link.Create'#.App'folder
r,←⊂ ⎕SE.Link.Import'#.DateTime'(parentfolder,'DateTime/APLSource')
r,←⊂ ⎕SE.Link.Import'#.CEF_Utility'(parentfolder,'CEF-Utility/APLSource')
r←⍪r
